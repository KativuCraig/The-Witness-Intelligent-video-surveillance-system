# Recommended base dependencies for integrating YOLOv8-based model in a Django backend
# Note: For CUDA/GPU support, install the matching torch + torchvision binary from https://pytorch.org/get-started/locally/ instead of the CPU wheel below.
Django>=4.2
djangorestframework>=3.14
firebase-admin>=6.0.0
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.7.0
Pillow>=9.4.0
numpy>=1.25.0
pandas>=2.2.0
python-dotenv>=1.0.0
# Async task queue (optional but recommended for long-running video inference)
celery>=5.3.0
redis>=4.5.0
# Production server (optional for deployment)
gunicorn>=20.1.0
# Type & compatibility helpers
typing_extensions>=4.5.0
